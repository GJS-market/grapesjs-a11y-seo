node_modules
# dist/ is committed: the single self-contained bundle (dist/index.js) is
# browseable on GitHub and servable via the jsdelivr /gh/ CDN path.
demo-dist
*.log
.DS_Store
coverage
test-results
playwright-report
.vite

# Local env (keep .env.example tracked)
.env
.env.*
!.env.example
